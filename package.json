{
  "name": "react-hooks",
  "version": "1.0.0",
  "description": "React Hooks",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:types": "npx lerna run build:types",
    "build": "npx lerna run --parallel build",
    "bootstrap": "npx lerna bootstrap",
    "lerna:init": "npx lerna init --independent"
  },
  "dependencies": {
    "typescript": "next",
    "ts-loader": "^8.3.0",
    "webpack": "^4.46.0",
    "webpack-cli": "^4.9.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.5.1"
  },
  "author": "Stanislav Panchenko",
  "license": "ISC"
}
